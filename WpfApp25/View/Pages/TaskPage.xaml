<Page x:Class="WpfApp25.View.Pages.TaskPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp25.View.Pages"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="700" 
      d:DesignWidth="900">

    <Border BorderBrush="{StaticResource PrimaryHueDarkBrush}" BorderThickness="3">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="170" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Image Grid.ColumnSpan="2"
                   Source="/Images/main_bg.jpg"
                   Stretch="UniformToFill"/>

            <Border Background="#FDFDFD"
                    Grid.Column="0"
                    BorderThickness="1"
                    BorderBrush="#DBDBDB"
                    Width="140"
                    CornerRadius="15"
                    Height="600">
                <StackPanel>
                    <Button Style="{DynamicResource menuButton}"
                            Margin="0 10 0 0"
                            x:Name="BtnTask1"
                            Click="BtnTask1_Click">
                        <StackPanel>
                            <materialDesign:PackIcon Kind="TaskAdd"
                                                     Style="{DynamicResource menuButtonIcon}" />
                            <TextBlock Text="Задание №1"
                                       Style="{DynamicResource menuButtonText}"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{DynamicResource menuButton}"
                            Margin="0 10 0 0"
                            x:Name="BtnTask2"
                            Click="BtnTask2_Click">
                        <StackPanel>
                            <materialDesign:PackIcon Kind="TaskAdd"
                                                     Style="{DynamicResource menuButtonIcon}" />
                            <TextBlock Text="Задание №2"
                                       Style="{DynamicResource menuButtonText}"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{DynamicResource menuButton}"
                            Margin="0 10 0 0"
                            x:Name="BtnTask3"
                            Click="BtnTask3_Click">
                        <StackPanel>
                            <materialDesign:PackIcon Kind="TaskAdd"
                                                     Style="{DynamicResource menuButtonIcon}" />
                            <TextBlock Text="Задание №3"
                                       Style="{DynamicResource menuButtonText}"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{DynamicResource menuButton}"
                            Margin="0 10 0 0"
                            x:Name="BtnTask4"
                            Click="BtnTask4_Click">
                        <StackPanel>
                            <materialDesign:PackIcon Kind="TaskAdd"
                                                     Style="{DynamicResource menuButtonIcon}" />
                            <TextBlock Text="Задание №4"
                                       Style="{DynamicResource menuButtonText}"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{DynamicResource menuButton}"
                            Margin="0 10 0 0"
                            x:Name="BtnTask5"
                            Click="BtnTask5_Click">
                        <StackPanel>
                            <materialDesign:PackIcon Kind="TaskAdd"
                                                     Style="{DynamicResource menuButtonIcon}" />
                            <TextBlock Text="Задание №5"
                                       Style="{DynamicResource menuButtonText}"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{DynamicResource menuButton}"
                            Margin="0 10 0 0"
                            x:Name="BtnLogout"
                            Click="BtnLogout_Click">
                        <StackPanel>
                            <materialDesign:PackIcon Kind="Power"
                                                     Style="{DynamicResource menuButtonIcon}" />
                            <TextBlock Text="Выйти"
                                       Style="{DynamicResource menuButtonText}"/>
                        </StackPanel>
                    </Button>

                </StackPanel>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="0 50 0 0"
                        Width="650"
                        Height="150"
                        Background="{StaticResource PrimaryHueMidBrush}"
                        CornerRadius="15"
                        Padding="5"
                        Effect="{StaticResource MaterialDesignElevationShadow1}">
                    <StackPanel HorizontalAlignment="Left"
                                VerticalAlignment="Center">
                        <TextBlock Text="Задание №1 - Создать класс с именем Adress. В теле класса требуется создать поля: index, country, city, street, house, apartment. Для каждого поля, создать свойство с двумя метода доступа. Создать экземпляр класса Address. В поля экземпляра записать информацию о почтовом адресе. Выведите MessageBox со значением полей, описывающих адрес."
                                   FontSize="17"
                                   Margin="10 0 0 0"
                                   TextWrapping="Wrap"
                                   Foreground="White"
                                   FontWeight="SemiBold" Width="595"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="0 10 0 0"
                        Width="650"
                        Height="140"
                        Background="{StaticResource PrimaryHueMidBrush}"
                        CornerRadius="15"
                        Padding="5"
                        Effect="{StaticResource MaterialDesignElevationShadow1}">
                    <StackPanel HorizontalAlignment="Left"
                                VerticalAlignment="Center">
                        <TextBlock Text="Задание №2 - Ввести два целочисленных массива - по 10 элементов в каждом. Сформировать новый массив, на четных местах которого будут элементы с нечетными индексами из первого массива, а на нечетных - с четными индексами из второго."
                                   FontSize="17"
                                   Margin="10 0 0 0"
                                   TextWrapping="Wrap"
                                   Foreground="White"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="2"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Top"
                        Margin="0 10 0 0"
                        Width="650"
                        Height="120"
                        Background="{StaticResource PrimaryHueMidBrush}"
                        CornerRadius="15"
                        Padding="5"
                        Effect="{StaticResource MaterialDesignElevationShadow1}">
                    <StackPanel HorizontalAlignment="Left"
                                VerticalAlignment="Center">
                        <TextBlock Text="Задание №3 - Ввести массив, состоящий из 8-ми элементов (восемь двузначных чисел) целого типа. Получить новый массив, состоящий из цифр, находящихся в младших разрядах элементов исходного массива."
                                   FontSize="17"
                                   Margin="10 0 0 0"
                                   TextWrapping="Wrap"
                                   Foreground="White"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="3"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Top"
                        Margin="0 10 0 0"
                        Width="650"
                        Height="80"
                        Background="{StaticResource PrimaryHueMidBrush}"
                        CornerRadius="15"
                        Padding="5"
                        Effect="{StaticResource MaterialDesignElevationShadow1}">
                    <StackPanel HorizontalAlignment="Left"
                                VerticalAlignment="Center">
                        <TextBlock Text="Задание №4 - Отсортировать по убыванию элементов последнего столбца целочисленный двухмерный массив 5х4."
                                   FontSize="17"
                                   Margin="10 0 0 0"
                                   TextWrapping="Wrap"
                                   Foreground="White"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="4"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Top"
                        Margin="0 10 0 0"
                        Width="650"
                        Height="70"
                        Background="{StaticResource PrimaryHueMidBrush}"
                        CornerRadius="15"
                        Padding="5"
                        Effect="{StaticResource MaterialDesignElevationShadow1}">
                    <StackPanel HorizontalAlignment="Left"
                                VerticalAlignment="Center">
                        <TextBlock Text="Задание №5 - Дана целочисленная матрица размера 5х5. Переставить местами 4 и 5 строку."
                                   FontSize="17"
                                   Margin="10 0 0 0"
                                   TextWrapping="Wrap"
                                   Foreground="White"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>



            </Grid>
        </Grid>
    </Border>
</Page>
